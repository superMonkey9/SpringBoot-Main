<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.pdemo.mapper.UserXmlMapper">

    <!-- 查询所有 -->
    <select id ="findAll" resultType="com.example.pdemo.entity.User">
        SELECT * FROM user
    </select>

    <!-- 根据 ID 查询 -->
    <select id="findById" resultType="com.example.pdemo.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <!-- 根据名字模糊查询 -->
    <select id="findByName" resultType="com.example.pdemo.entity.User">
        SELECT * FROM user WHERE name LIKE CONCAT('%', #{name}, '%')
    </select>

    <!-- 插入 -->
    <insert id="insert" parameterType="com.example.demo.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(name, age, email)
        VALUES(#{name}, #{age}, #{email})
    </insert>

    <!-- 修改 -->
    <update id="update" parameterType="com.example.demo.entity.User">
        UPDATE user
        SET name = #{name}, age = #{age}, email = #{email}
        WHERE id=#{id}
    </update>

    <!-- 删除 -->
    <delete id="delete">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <!-- 动态条件查询 -->
    <select id="findByCondition" resultType="com.example.demo.entity.User">
        SELECT * FROM user
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="age != null">
                AND age = #{age}
            </if>
            <if test="email != null and email != ''">
                AND email = #{email}
            </if>
        </where>
    </select>

    <!-- 动态更新：只更新传了值的字段 -->
    <update id="dynamicUpdate" parameterType="com.example.demo.entity.User">
        UPDATE user
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="age != null">age = #{age},</if>
            <if test="email != null">email = #{email},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 批量删除 -->
    <delete id="batchDelete">
        DELETE FROM user WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 批量插入 -->
    <insert id="batchInsert">
        INSERT INTO user(name, age, email) VALUES
        <foreach collection="users" item="user" separator=",">
            (#{user.name}, #{user.age}, #{user.email})
        </foreach>
    </insert>


    <!--    参数解释：-->
    <!--    collection — 要遍历的集合-->
    <!--    item — 每个元素的变量名-->
    <!--    open — 开始符号-->
    <!--    separator — 分隔符-->
    <!--    close — 结束符号-->

</mapper>